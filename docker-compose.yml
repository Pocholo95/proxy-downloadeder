services:
  proxy-downloader:
    build: .
    image: proxy-downloader-webui
    container_name: proxy-downloader
    ports:
      - "${WEB_PORT:-8080}:8080"
    volumes:
      - ${DOWNLOADS_PATH:-./downloads}:/downloads
      - ${CONFIG_PATH:-./config}:/app/config
      - ${STATE_PATH:-./state}:/app/state
    restart: unless-stopped
